{
  "name": "cardioide-web",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "serve": "parcel src/index.html --out-dir dist --no-cache",
    "watch": "parcel watch src/index.html --out-dir dist --no-cache",
    "build": "parcel build src/index.html --out-dir dist --no-cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.9.4",
    "sass": "^1.14.3"
  },
  "dependencies": {
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.4"
  }
}
